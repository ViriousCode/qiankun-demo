### 💡 这是一个变量定义，方便全局切换环境
@baseUrl = http://localhost:3000
@token = mock-dev-token-123

### 1. 获取工作台列表 (GET 请求示例)
# 只要写出 GET 和 URL 即可，注意上一行必须用 ### 分隔不同的请求
GET {{baseUrl}}/api/workbench/list
Authorization: Bearer {{token}} 


### 2. 模拟登录获取 Token (POST 请求示例)
POST {{baseUrl}}/api/login
Content-Type: application/json

{
    "username": "admin",
    "password": "123"
}


### 3. 添加一个工作台卡片 (带 Body 的 POST)
POST {{baseUrl}}/api/workbench/add
Content-Type: application/json
Authorization: Bearer {{token}}

{
    "title": "新测试应用",
    "path": "/test-sub-app/new",
    "targetType": "internal",
    "category": "业务系统"
}